export interface HeroProps {
  heroTag: string;
  subTag: string;
  tagLine: string;
  button1: {
    link: string;
    text: string;
  };
  button2: {
    link: string;
    text: string;
  };
  heroImage: string;
}
